# HMO 项目工作原则

本文档定义本仓库内论文探索、实验开发、结果沉淀的协作规则。

## 1. 工作流程总原则

**先理解，再规划，再实现，每步验证，不跳过步骤。遇到阻塞就回退重新思考原因。**

### 1.1 Idea 开发

- 开始前识别不确定性和假设，不确定的部分追问用户，不要自行假设。
- 大目标拆分为可验证的小步骤，每步有 go/no-go 判据。
- 先了解已有实现与进展，避免重复工作或与已有结论矛盾。

### 1.2 代码实现

- 实现前阅读相关约束文件，理解当前约定和上下文。
- 一次只实现一件事，实现完毕后立即验证。不堆叠多个改动后再统一验证。
- 禁止防御性垃圾代码：不堆叠冗余 fallback、无意义 wrapper、宽泛 try/except 或不会被使用的抽象。
- 优先沿用现有项目结构和风格，每个新增函数、参数、文件都应能说明其目的。

### 1.3 验证

- 确保实现按照用户给出的方向与要求来。有自己增加的改动必须明确说明。
- 每完成一个最小变更立即验证，不积累未验证的改动。
- 验证失败则回退到上一阶段重新思考，必要时追问用户。

### 1.4 长实验运行

- 长时间实验必须在命名清晰的 detached `screen` 会话中运行。
- 启动后核对 `screen -ls`、进程状态、结果增长和错误日志。
- stdout/stderr 持久化到日志文件；实验结束后释放 GPU。

## 2. 结果目录边界

`experiments/results/` 用于存放总结性结果（图表、结论 `.md`、汇总材料）。不得将完整训练输出或大规模中间结果放入其中。

大体量输出放在外存 `/mnt/nvme0` 下，仓库内只保留 symlink 或轻量路由入口。

## 3. 论文导向原则

**代码实现和论文写作故事是不一样的，以论文故事为准。**

- 代码和理想设计之间有 gap 是可以接受的。论文只看最终效果。
- 设计文档是参考，不是实现规范。实现可以比设计简单或复杂，取决于效果。
- 论文中以最终提交版本为准，不需要解释中间迭代的设计变体。
- 不要为了让代码"看起来和论文描述一致"而引入不必要的复杂度。

### 3.1 叙事结构

论文叙事从一般性现象出发，逐层深挖，逻辑严格递进。不允许一上来就框定到具体子领域然后框内解题。

- 每句承接上句结论或回答上句留下的问题。
- 每段回答上段的自然追问，并留下下一段需要回答的问题。
- 不允许"我们发现了 X 问题，所以我们提出了 Y 方法"的两段式跳跃——中间推导必须写出来。

### 3.2 写作风格

- 拒绝过多防御性语句。正文叙事应自信、流畅、有推进感。
- 不在正文中大量使用 hedging / caveat / disclaimer，它们属于 limitation section。
- 面对审稿人可能的质疑，用更强的证据和更紧的逻辑让质疑自然消解。

### 3.3 Method 设计

- Method 必须以主动赢得核心指标为目标，不是以"不出错、不掉点"为目标。
- 如果设计自然收敛成防守型方案，必须回到 problem definition 重新设计。
- 安全 gate、rollback 等只允许作为执行层风险控制，不能成为核心贡献。

### 3.4 原型开发与贡献分离

已有工作只是原型开发基础，contribution framing 必须强调我们自己的新问题定义、新机制和新证据链。已有工作在 related work 中正常讨论、在 baseline 中公平比较。

## 4. 验证真实性（硬约束）

用于判断 idea 生死的验证，测量的量必须是真实的，不允许用模拟、近似或 proxy 代替被测对象本身。

当真实测量代价过高时，正确做法是缩小规模（更少场景/更短 rollout），而不是把被测量替换成近似。
